# Push all branches
    $ git push --all
# Push current branch
    $ git push
# Push current branch to another branch
    $ git push origin master
# Push files to remote repository
    $ git push origin master